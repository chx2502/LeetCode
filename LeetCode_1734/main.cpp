#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> decode(vector<int>& encoded) {
        unsigned long size = encoded.size();
        if (size == 0) return {};
        vector<int> result;

        for (const int num : encoded) {
            
        }
    }
};
